\documentclass[../UC.tex]{subfiles}

\begin{document}

\begin{table}[h]
\centering
\begin{tabular}{|p{4cm}|p{10cm}|}
\hline
\textbf{Mã UC} & UC10-05 \\
\hline
\textbf{Tên UC} & Phân tích Dữ liệu Lịch sử (Analyze Historical Data) \\
\hline
\textbf{Mô tả tóm tắt} & (Hệ thống) Quét lịch sử xuất kho để tìm ra các xu hướng nhu cầu. \\
\hline
\textbf{Tác nhân tham gia} & System (Hệ thống - Tác vụ tự động) \\
\hline
\textbf{Luồng sự kiện chính} & 
\begin{enumerate}
    \vspace{-18pt}
    \item Hệ thống truy vấn lịch sử các đơn hàng xuất kho (Outbound Order History).
    \item Hệ thống tính toán các mẫu nhu cầu (demand patterns).
    \item Hệ thống áp dụng các phương pháp dự báo (VD: Trung bình trượt, Điều chỉnh theo mùa...).
\end{enumerate} \\
\hline
\textbf{Luồng sự kiện khác} & Không có \\
\hline
\textbf{Yêu cầu đặc biệt} & Tác vụ phải chạy ngầm, không ảnh hưởng hiệu suất ban ngày. \\
\hline
\textbf{Trạng thái hệ thống ngay khi bắt đầu thực hiện} & Tác vụ được lập lịch (cron job) và có đủ dữ liệu lịch sử. \\
\hline
\textbf{Trạng thái hệ thống sau khi bắt đầu thực hiện} & Dữ liệu phân tích sẵn sàng cho UC10-08. \\
\hline
\textbf{Điểm mở rộng} & Không có \\
\hline
\end{tabular}

\caption{Bảng UC10-07}
\end{table}

\end{document}

